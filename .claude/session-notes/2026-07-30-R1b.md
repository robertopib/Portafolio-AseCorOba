## R1b — Fix CMS image uploads (413 / >4.5MB) via direct-to-R2 client uploads — 2026-07-30

### Context
First worker task run through the new conductor workflow. Diagnosis: uploads failed
with 413 FUNCTION_PAYLOAD_TOO_LARGE (Vercel ~4.5 MB fn body limit; images are 11–18 MB).

### Content
**Status: partial** — code shipped to `preview` (commit `987b58c`); end-to-end upload
test pending the human R2 CORS step + preview redeploy.

Changes:
- `cms/src/payload.config.ts` — `clientUploads: true` on the s3Storage plugin
  (browser PUTs directly to R2, bypassing the fn limit). Serving path unchanged.
- `cms/src/migrations/index.ts` — removed dangling import of the never-committed
  `20260730_133343_testdelta` migration.

**Critical finding (→ R8):** the phantom `testdelta` import (introduced in `2095df8`,
still on `main`) breaks `next build`, so the CMS `ci:build` has failed on every push
since then → Vercel served the last good deploy → **migrations Tier 1 never actually
went live**; prod CMS still serves `d019791` (label feature; schema was applied
manually). Preview fixed by `987b58c`; `main` fix rides the eventual R1b→main merge.

Verification: CMS build PASS (after index.ts fix); `generate:types` no drift;
`migrate:status` = baseline only (no pending); no migration needed (clientUploads is
plugin config, no columns).

Blockers (human):
1. Apply R2 CORS to the media bucket (Cloudflare → R2 → media → Settings → CORS):
   AllowedOrigins = cms-preview + cms admin origins; Methods PUT+GET; Headers *;
   Expose ETag; MaxAge 3600.
2. After preview redeploy of `987b58c` + CORS: upload ~15 MB and small image on
   cms-preview/admin; confirm Media doc + serves from media.ase-cor-oba.site; confirm
   preview site still loads.

### Update — 2026-07-30 (VERIFIED, done on preview)
After applying R2 CORS + removing a **leading space in the Vercel `S3_BUCKET`** env
var (` asecoroba-media` → `asecoroba-media`; presign was hitting `/%20asecoroba-media/`
→ 400 preflight) + redeploy (`d462e56`): large (~15 MB) AND small uploads **succeed**
on cms-preview/admin. R1b done on preview. Images show without thumbnails (expected →
R1c). Not yet on prod (rides the R1→main merge with R8).
New guard idea → R2 (CI gate): validate/trim env vars to catch stray spaces.

### Expiry
Expires when R1 (upload + thumbnails + backfill) is fully done and merged to prod.
