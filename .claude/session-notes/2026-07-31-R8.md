## R8 — Prod rollout of R1 + R10 (+ restore prod CMS build) — 2026-07-31

### Context
Ship the verified image uploads + thumbnails + library column to production, and in
the same deploy fix the prod CMS build that had been broken since the phantom
`testdelta` import (so migrations Tier 1 becomes genuinely live).

### Content
**Status: done (PROD).** Authorized: `authorize production deploy` +
`authorize db migration on production`.

Sequence executed:
1. `pnpm migrate:prod` (direct endpoint) applied `20260730_192249_add_media_image_sizes`
   to prod (184ms) — done BEFORE the merge so the build's pooled `migrate` is a no-op
   (avoids pooler-DDL risk). Prod status: baseline + image_sizes both Ran: Yes.
2. Merged `preview`→`main` (`db0109c`, 16 commits, no conflicts) + pushed. Prod CMS
   `ci:build` succeeded — FIRST green prod build since `2095df8` (phantom import gone
   via preview's `987b58c`). Confirmed new code live by `sizes` field on prod
   `/api/media` (~t+100s).
3. Prod thumbnail backfill (`.env.prod`, direct, dbGuard): **42/42 processed, 0
   failed** (report `/tmp/backfill-thumbnails-report.json`).
4. `reset-media-list-prefs.ts` on prod: deleted 2 `collection-media` prefs (column
   shows by default).

Verify: prod media `thumbnailURL` = `https://media.ase-cor-oba.site/…-400x…webp`,
serves 200 image/webp; prod `/api/pages` 200; `www.ase-cor-oba.site` 200. Front-end
code unchanged → public site pixel-identical by construction.

### Remaining (human)
- **Confirm prod live upload:** log into `cms.ase-cor-oba.site/admin`, upload a large
  image, confirm no 400 (this exercises Vercel's PROD `S3_BUCKET` env — must have no
  leading space; the backfill used `.env.prod`'s clean value so it didn't test Vercel's).
- Publish (public site rebuild) if you want any content refreshed.

### Expiry
Permanent record of the prod rollout; supersedes the R1b/R1c/R1d/R10 preview notes.
